---
// Provas — the commercial trust engine. Real, named testimonials from people
// who operate under risk. Surface: soft. First card is featured (spans wider)
// for rhythm. Quotes are verbatim (translated per locale). Brand is always "iarandu".
import { useTranslations, toLang } from "../../i18n/ui";

const t = useTranslations(toLang(Astro.currentLocale)).provas;
---

<section id="depoimentos" class="provas surface-soft sec-lg">
  <div class="wrap">
    <header class="provas-head">
      <div class="badge reveal">
        <span class="badge-dot" aria-hidden="true"></span>
        {t.badge}
      </div>
      <h2 class="h2 provas-h" data-words>{t.h}</h2>
      <p class="body-lg measure provas-intro reveal" style="transition-delay:120ms;">
        {t.intro}
      </p>
    </header>

    <div class="provas-grid">
      {t.items.map((it, i) => (
        <figure
          class:list={["card", "provas-card", { "provas-card-feat": i === 0 }, "reveal"]}
          style={`transition-delay:${80 + i * 100}ms;`}
        >
          <span class="provas-quote" aria-hidden="true">&ldquo;</span>
          <blockquote class="provas-text">{it.quote}</blockquote>
          <figcaption class="provas-attr">
            <span class="provas-name">{it.name}</span>
            <span class="label provas-role">{it.role}</span>
          </figcaption>
        </figure>
      ))}
    </div>
  </div>
</section>

<style>
  .provas-head { max-width: 56rem; }
  .provas-h { margin: 18px 0 0; }
  .provas-intro { margin: 20px 0 0; }

  .provas-grid {
    margin-top: clamp(40px, 5vw, 64px);
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
  }
  @media (min-width: 980px) {
    .provas-grid { grid-template-columns: repeat(3, 1fr); }
    /* featured card spans two columns for rhythm */
    .provas-card-feat { grid-column: span 2; }
  }

  .provas-card {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 18px;
    padding-top: 26px;
  }

  .provas-quote {
    font-family: "Fraunces", Georgia, serif;
    font-weight: 700;
    font-size: 96px;
    line-height: 0.6;
    color: var(--urucum);
    height: 44px;
    display: block;
  }

  .provas-text {
    margin: 0;
    font-family: "Inter Tight", system-ui, sans-serif;
    font-weight: 500;
    font-size: clamp(18px, 1.4vw, 21px);
    line-height: 1.5;
    letter-spacing: -0.008em;
    color: var(--ink);
  }
  .provas-card-feat .provas-text {
    font-size: clamp(21px, 1.9vw, 27px);
    line-height: 1.42;
  }

  .provas-attr {
    margin-top: auto;
    display: flex;
    flex-direction: column;
    gap: 5px;
  }
  .provas-name {
    font-family: "Inter Tight", system-ui, sans-serif;
    font-weight: 700;
    font-size: 17px;
    letter-spacing: -0.006em;
    color: var(--ink);
  }
  .provas-role { color: var(--ink-muted); }
</style>
